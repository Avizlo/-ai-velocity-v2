export const metadata = {
    title: 'Agentic Strategy | Ai Velocity Foundry',
    description: 'Agentic Strategy operates via autonomous loops, running millions of competitive simulations per second to identify unavoidable checkmate scenarios.',
};

export default function Layout({ children }) {
    return children;
}
